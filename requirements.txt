# Core dependencies
requests==2.31.0
aiohttp==3.9.0
python-dateutil==2.8.2
discord.py==2.3.0

# Optional: HTML parsing (if needed)
beautifulsoup4==4.12.0

# Optional: Summarization (free tiers)
# Google Gemini (free: 1,500 req/day)
google-generativeai==0.3.0

# Groq (free: 30 req/min)
groq==0.4.0

# Optional: Claude summaries (if using paid tier)
anthropic==0.7.0

# Development & utilities
pytest==7.4.0
python-dotenv==1.0.0

# Note: Discord integration uses curl subprocess (no library needed)
